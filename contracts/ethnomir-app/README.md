# Договор на разработку ethnomir.app — рабочий черновик

Папка для итерации проекта рабочего договора между Billions X (ИП Иванов Е. В.) и ООО «ЭТНО-ДЕРЕВНЯ».

Статус: **черновик v6 · апрель 2026**.

## Структура папки

| Файл | Назначение |
|---|---|
| `Договор_ethnomir_app_v6.md` | **Актуальная версия.** Markdown — основной рабочий формат. |
| `Договор_ethnomir_app_v6.docx` | Word-версия v6 (SF Display заголовки + SF Text основной). |
| `Договор_ethnomir_app_v6.pdf` | PDF-версия v6 для рассылки и печати — рекомендуется как эталон, потому что рендерится одинаково независимо от установленных шрифтов на машине получателя. |
| `Договор_ethnomir_app_v5.*`, `v4.*`, `v3.2.*`, `v3.1.*`, `v3.*`, `v2.*`, `v1.*` | Предыдущие версии для истории правок. |
| `build_docx.py` | Скрипт сборки DOCX из Markdown с пост-обработкой таблиц. |

## Что заложено в v4

| Параметр | Значение |
|---|---|
| Номер / дата | **№ 04-05/2026** от **04 мая 2026 г.** (понедельник; формат номера = дата подписания DD-MM/YYYY как в образцах). |
| Исполнитель | ИП Иванов Евгений Владимирович, ИНН 911002153904, ОГРНИП 325911200092110. |
| Заказчик | ООО «ЭТНО-ДЕРЕВНЯ» в лице управляющего ИП Черновой С. А. (договор о передаче полномочий № 01/24/ЭД/ПП от 29.08.2024). |
| Подписи (полные ФИО) | «/ Иванов Евгений Владимирович /» — Исполнитель; «/ Чернова Светлана Александровна /» — Заказчик. |
| Общая сумма | 22 000 000 ₽, НДС не облагается. |
| Этапы | 50 % / 25 % / 25 % + 4-й этап бесплатного сопровождения. |
| Срок | ≈ 9 месяцев. |
| Шрифт DOCX | SF Display (заголовки) + SF Text (основной), строгий ч/б, без разделительных линий. |
| Структура | Тело договора + три Приложения, каждое с **отдельной страницы**. |

## Что изменено в v6 по сравнению с v5

Версия v6 завершает сверку **всего реализованного** клиентского функционала и CRM-режима с исходным кодом и production-БД. v5 покрывала только Tab Bar-вкладки (3.1–3.5, 3.7); v6 добавляет всё, что осталось «на скриншотах» или вообще отсутствовало в договоре.

| Раздел | Что было в v5 | Что в v6 |
|---|---|---|
| **3.6. М05. Чеки и QR** | Общее описание + декларативный формат `ETM-YYMMDD-XXXXX`. | Привязка к реальным таблицам `orders` / `receipts` / `receipt_items`; компоненты `OrderView` (строки 10151–10310) и view `receipts` в Паспорте; точные статусы чека из кода (`pending` / `confirmed` / `paid` / `completed` / `cancelled` / `refunded`); формат `order_code` отвязан от вымышленного шаблона; добавлено упоминание счётчиков `receipt_views` и `receipt_viewed_at`. |
| **3.9. М08. Календарь событий** | Декларация «4 категории» (festival/holiday/kids/event) и «20 тематических недель». | Привязка к компоненту `CalendarView` (строки 651–731) и таблице `park_events` (34 записи); цветовая палитра категорий взята из кода (`festival` = синий, `holiday` = красный, `kids` = зелёный, иное = оранжевый); отмечено, что многодневные события подсвечиваются на каждом дне `date_start..date_end`. |
| **3.10. М09. Сертификаты** | 4 номинала, общие правила. | Привязка к таблице `gift_certificates` со схемой полей; статусы (`active` / `partially_used` / `used` / `expired`); поддержка частичного списания (`balance`); отображение в разделах Паспорта. |
| **3.11. М10. B2B-направление** | «Минимум 16 площадок для MICE». | Привязано к таблице `b2b_programs` (3 активные программы на момент сборки), точное описание формы заявки (имя/телефон/число чел./комментарий), интеграция с `contact_requests` и CRM «Лиды». |
| **3.12. М11. Отзывы и NPS** | Цели и общая логика. | Добавлены источники: `reviews` (универсальная), `restaurant_reviews`, `hotel_reviews`; поле `moderation_status` и поток модерации; поля `manager_response` / `response_at` для официального ответа администрации. |
| **3.13. М12. Уведомления** | 3 канала + список таблиц. | Уточнённые имена таблиц (`push_subscriptions`, `push_tokens`, `push_messages`, `sms_rate_limits`, `notifications`, `crm_notifications`); связь с маркетинговыми сущностями (`promos` / `promo_banners` / `promo_codes` / `promo_code_uses`). |
| **3.14. М13. Глобальный поиск** | «Минимум 150 объектов контента» + «10 популярных запросов». | Привязка к компоненту `SearchModal` (строки 8444–8548); точный список **9 категорий** (отели / туры / рестораны / услуги / мастер-классы / события / страны / статьи / FAQ); до 5 элементов на категорию (3 для статей); фактический индекс — ≥ 270 объектов на момент сборки. |
| **3.15. Сквозные компоненты и оверлеи (новый раздел)** | — | **Полностью новая секция:** Tab Bar, Splash + Onboarding, Auth Overlay (Phone OTP + Email/пароль), Search, Cart Sheet, Checkout Sheet, Order View, Park Map, Calendar, QR Scanner, Country Detail, Booking Modal, Chat, Notifications, Promo, iOS Alert / Confirm, A2HS-prompt, Offline Indicator, PWA-манифест, BillionsX Overlay, 16 LandingV2-компонентов. Полный реестр всего, что монтируется на корневом уровне `App` помимо вкладок. |
| **4. CRM — режим «Владелец»** | 23 модуля в 8 доменах. | **29 модулей** — реальный список из `apps/web/app/page.tsx:3871`: добавлены `timeline`, `requests`, `payments`, `pricing`, `map`, `shifts`, `surveys`, `ai`, `documents`. Каждый модуль теперь привязан к ключу `crmSection`, источникам данных и RPC. Уточнена RBAC: 28 поддерживаемых ролей (заполнено 23 в `crm_staff`); карта `canDo` явно указана в коде и зафиксирована в договоре. |

Реквизиты, сумма (22 000 000 ₽), сроки, стороны и подписи — без изменений.

## Что изменено в v5 по сравнению с v4

Главная цель версии — заменить описания клиентских модулей договора, сделанные «по скриншотам», на полную сверку с исходным кодом приложения (`apps/web/app/page.tsx`, ветка `main`, SHA на момент сборки) и актуальными значениями в production-БД Supabase (`ewnoqkoojobyqqxpvzhj`).

| Раздел | Что было в v4 | Что в v5 |
|---|---|---|
| **3.1. М00. Главная «Сегодня»** | 6 пунктов «Минимально обязательные экраны и элементы». | Полное описание **18 блоков** в порядке рендера компонента `HomeTab` (строки 797–1565), включая 5 Premium-баннеров с анимациями, Discover-сетку из 7 карточек, погоду со звонком и навигатором, расписание дня с live-индикатором, и т.д. |
| **3.2. М01. Парк и билеты** | 8 суб-разделов; B2B заявлен как «16 площадок для MICE». | Фактически **10 суб-разделов** (`tickets`, `tours`, `mk`, `events`, `excursions`, `museums`, `schedule`, `certificates`, `b2b`, `calendar`); `b2b_programs` в БД — 3 шт.; добавлено описание detail-карточки с пятью типами `landing_sections`. |
| **3.3. М02. Жильё** | Общее описание 3 разделов. | Те же 3 раздела (`hotels`/`guest`/`re`), но добавлено детальное описание **Hotel Detail View** (галерея, типы номеров, платные услуги, правила, рядом, отзывы, перекрёстная навигация, расширенный блок бронирования с селекторами гостей и детей, бейдж «питомцы», блок «Включено в стоимость»). |
| **3.4. М03. Услуги** | 6 суб-разделов с приблизительной детализацией. | Те же 6 суб-разделов, но числа сверены с БД: `delivery_items` = 25, `restaurants` = 18, `menu_items` = 166, `services.banya` = 5, `services` фан-категории = 26, `services.shop` = 7, `services.rental` = 4. Добавлен режим «Гастро-коллекция». |
| **3.5. М04. Паспорт путешественника** | Таблица «15 экранов внутри». | Точный реестр **16 view-режимов** компонента `PassportView` (`countries`, `regions`, `achievements`, `bookings`, `orders`, `receipts`, `favorites`, `requests`, `reviews`, `wallet`, `points`, `settings`, `privacy`, `terms`, `crm`, `collections`); правила баллов привязаны к таблице `points_rules` (18 правил на момент сборки). |
| **3.7. М06. Этномир-хаб** | Декларация «22 лендинга в трёх пакетах 7/5/10». | Реальная структура из кода `EthnoMirTab` (строки 7220–8219): Hero-card с многослойной анимацией, блок «Новости» (карусель из `articles`), три списка лендингов **7/5/5 = 17** (точные `slug`), блок «Поддержка», footer. Дополнительно — полный реестр **22 `landing_pages`** из БД с указанием, какие из них рендерятся в других модулях (`gift`, `corp`, `educ`, `travel`, `ethnomir3000`). |

Цифры в реквизитах, сумма (22 000 000 ₽), сроки (≈ 9 месяцев), стороны и подписи — без изменений.

## Что изменено по сравнению с v3.2

1. **Дата** изменена с 07.05.2026 на **04 мая 2026 г.** Номер договора синхронно изменён на **№ 04-05/2026** во всех 7 местах.
2. **Подписи** в разделе 17 и в форме Акта — теперь содержат **полные ФИО** Исполнителя и Заказчика как в реквизитах (раньше были сокращения «Иванов Е. В.» / «Чернова С. А.»).
3. Каждое **Приложение** (№ 1, № 2, № 3) теперь начинается **с новой страницы** — добавлены явные разрывы страницы.
4. **Сверка с справочником продукта** — обновлены формулировки без увеличения объёма работ:
   - предмет договора уточнён: указано, что приложение функционально замещает PMS отелей, POS ресторанов, CRM, маркетплейс услуг, админ-панели контента и инвестиционной недвижимости в едином контуре;
   - в M03 «Услуги» явно упомянут **трансфер**;
   - в реестре CRM модуль «Analytics» дополнен **RFM-сегментацией**;
   - в M00 «Главная» уточнено, что **промо-баннеры** ведут переходом на конкретные экраны приложения;
   - в правилах геймификации явно добавлено **сгорание баллов по сроку давности** (управляется через CRM);
   - убрано прямое имя внутренней таблицы (`crm_partner_access`) в 4.6 — заменено на функциональное описание партнёрского доступа.

## Открытые вопросы

1. Гарантийные обязательства (12 мес. сверх Этапа 4) — оставить, расширить, сократить?
2. Передача РИД — копировать ли развёрнутый список из образца №02-09/2025 (п. 2.2) в раздел 9?
3. График оплат — Этномир может попросить меньший аванс (30 %) — обсуждаемо.
4. Контактный e-mail Заказчика — два места `[УТОЧНИТЬ]`.
5. Юридическая экспертиза — обязательно перед подписанием.

## История версий

| Версия | Дата | Изменения |
|---|---|---|
| v1.0 | апр. 2026 | Первый черновик. |
| v2.0 | апр. 2026 | ИП вместо НПД; новый № полномочий 01/24; убрано наполнение лендингов в ТЗ. |
| v3.0 | апр. 2026 | Дата 07.05.2026; SF Pro; убран AI-чат, PRO, WCAG, конкретные QR; смягчён стек; упрощены приёмка и SLA; убраны пени; производительность ×10. |
| v3.1 | апр. 2026 | № договора 07-05/2026; шрифт SF Display / SF Text. Содержание v3 не менялось. |
| v3.2 | апр. 2026 | Исправлена вёрстка 13 таблиц в DOCX; добавлен PDF. |
| v4 | апр. 2026 | Дата 04.05.2026; № 04-05/2026; полные ФИО в подписях; каждое приложение с новой страницы; уточнения в ТЗ по сверке со справочником продукта (PMS/POS, трансфер, RFM, сгорание баллов, deeplink-баннеры). |
